import React from "react";

/**
 * Untertitel-Komponente für H1
 * @param children
 * @returns {JSX.Element}
 * @constructor
 */
export function Subtitle({ children }) {
  return <div className="h1-subtitle">{children}</div>;
}
